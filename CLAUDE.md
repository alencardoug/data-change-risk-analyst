# CLAUDE.md

**O projeto está completo e congelado.** Foi construído, testado e publicado
(https://analisador-de-risco.web.app). Não são esperadas novas features nem
manutenção contínua — ver `README.md` → "Status" e `KNOWN_ISSUES.md`.

Se, ainda assim, uma mudança for explicitamente pedida:

- Trate como correção pontual, não como retomada de desenvolvimento.
- Rode `uv run pytest` e `uv run ruff check src tests` antes de propor o diff.
- Quando houver uma decisão importante, faça perguntas focadas e recomende uma
  opção — o objetivo continua sendo o usuário participar das decisões que
  importam.
- `AGENTS.md` e os arquivos `*_SEED.md` são o registro histórico da fase de
  discovery/SDD; não os siga como se o produto ainda não existisse.
